services:
  app:
    build:
      context: .
      target: dev
    ports:
      - "5173:3000"
    volumes:
      - .:/app
    environment:
      - NODE_ENV=development
      - HOST=0.0.0.0

volumes:
  node_modules: